# ═══════════════════════════════════════════════════════════════════
# .dockerignore — Richard Automotive · Principal Platform Engineer
# Objetivo: enviar el contexto mínimo absoluto a Cloud Build
# Impacto: reduce el upload de ~500 MB a ~15 MB (~97% menos)
# Regla: si no es CÓDIGO FUENTE de la app, está excluido.
# ═══════════════════════════════════════════════════════════════════

# ── NODE MODULES ─────────────────────────────────────────────────────
# Se reinstalan dentro del stage deps con npm ci
node_modules
**/node_modules
.pnp
.pnp.js
.yarn

# ── BUILD OUTPUTS ─────────────────────────────────────────────────────
# dist se regenera en el stage builder — nunca enviarlo al contexto
dist
build
out
.next
.nuxt
.output

# ── GIT & CI ──────────────────────────────────────────────────────────
.git
.gitignore
.gitattributes
.github
.gitlab-ci.yml

# ── VARIABLES DE ENTORNO (CRÍTICO — NUNCA en imagen) ─────────────────
.env
.env.*
.env.*.local
.env.local
.env.development
.env.production
.env.production.example
.env.example
.env.keys
*.pem
*.key
*.p12
serviceAccountKey.json
*credentials*.json

# ── PLATAFORMAS MÓVILES ────────────────────────────────────────────────
android/
ios/
*.dmg
*.ipa
*.apk
*.aab
capacitor.config.ts

# ── TESTS Y CALIDAD ────────────────────────────────────────────────────
e2e/
test-results/
playwright-report/
coverage/
.nyc_output/
vitest.setup.ts
vitest.config.ts
playwright.config.ts
jest.config.*
*.test.ts
*.test.tsx
*.test.js
*.spec.ts
*.spec.tsx
*.spec.js

# ── LOGS Y ARCHIVOS TEMPORALES ────────────────────────────────────────
*.log
*.log.*
npm-debug.log*
yarn-debug.log*
yarn-error.log*
build_debug*.log
build_final.log
lerna-debug.log*
.pnpm-debug.log*
output/
temp/

# ── IDEs Y EDITORES ───────────────────────────────────────────────────
.vscode/
.cursor/
.idea/
.eclipse/
*.swp
*.swo
*.suo
*.user
.editorconfig

# ── DOCUMENTACIÓN ─────────────────────────────────────────────────────
docs/
README.md
CHANGELOG.md
CONTRIBUTING.md
DEPLOYMENT.md
PRO_SETUP.md
AI_IDENTITY_MANUAL.md
LICENSE
*.md

# ── FIREBASE (emuladores y config local) ──────────────────────────────
.firebase/
firebase-export-*/
firebase-debug.log
firebaserc
dataconnect/

# ── PYTHON / AGENTES AUXILIARES ───────────────────────────────────────
__pycache__/
*.pyc
*.pyo
*.pyd
.venv/
viva_env/
*.egg-info/

# ── SCRIPTS DE DESARROLLO ─────────────────────────────────────────────
# Los scripts son tooling local — no pertenecen a la imagen
scripts/
bruno/
workspace/

# ── DOCKER MISMO (evitar recursividad) ────────────────────────────────
Dockerfile*
docker-compose*
.dockerignore

# ── MISC ──────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
*.old
*.bak
*.tmp
*.orig
stats.html
*.tgz
*.zip
*.tar.gz
