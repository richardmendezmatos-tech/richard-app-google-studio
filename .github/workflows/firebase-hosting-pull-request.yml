# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on PR
'on': pull_request
concurrency:
  group: firebase-hosting-pr-${{ github.event.pull_request.number }}
  cancel-in-progress: true
env:
  DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
  VITE_FIREBASE_API_KEY: ci-placeholder-key
  VITE_FIREBASE_AUTH_DOMAIN: ci-placeholder.firebaseapp.com
  VITE_FIREBASE_PROJECT_ID: ci-placeholder
  VITE_FIREBASE_STORAGE_BUCKET: ci-placeholder.appspot.com
  VITE_FIREBASE_MESSAGING_SENDER_ID: "1234567890"
  VITE_FIREBASE_APP_ID: "1:1234567890:web:abcdef123456"
  VITE_FIREBASE_MEASUREMENT_ID: G-TEST12345
  VITE_GEMINI_API_KEY: ci-placeholder-gemini
jobs:
  build_and_preview:
    timeout-minutes: 60
    if: '${{ github.event.pull_request.head.repo.full_name == github.repository }}'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: Lint
        run: npm run lint
      - name: Unit tests
        run: npm run test
      - name: Install Playwright Chromium
        run: npx playwright install --with-deps chromium
      - name: Boot smoke test
        run: npm run test:e2e:smoke
      - name: Build
        run: npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_RICHARD_AUTOMOTIVE }}'
          projectId: richard-automotive
      - name: Notify Discord on preview failure
        if: failure() && env.DISCORD_WEBHOOK_URL != ''
        run: |
          curl -sS -X POST "$DISCORD_WEBHOOK_URL" \
            -H "Content-Type: application/json" \
            -d "{\"content\":\"🚨 PR preview deploy failed in ${GITHUB_REPOSITORY} (#${{ github.event.pull_request.number }}). Run: ${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}\"}"
